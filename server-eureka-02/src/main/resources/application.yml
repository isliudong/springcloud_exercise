server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com #服务端实例名
  client:
    register-with-eureka: false #排除自身注册
    fetch-registry: false #不检索服务，只维护服务
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka